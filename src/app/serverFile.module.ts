export class serverFile{
    server_filename: string;
    filename: string;

    constructor(server_filename:string, filename:string){
        this.server_filename = server_filename;
        this.filename = filename;
    }
}